package com.android.deviceops.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & text ─────────────────────────────────────────────────────
val Background      = Color(0xFF000000)
val PrimaryText     = Color(0xFFFFFFFF)
val SecondaryText   = Color(0xFFB7B7BB)

// ── Buttons ───────────────────────────────────────────────────────────────
val ButtonActive    = Color(0xFF4269FF)   // 可按
val ButtonInactive  = Color(0xFF173166)   // 不可按 (disabled)

// ── Chip / list background ────────────────────────────────────────────────
val ChipBackground  = Color(0xFF252528)

// ── Radio button ──────────────────────────────────────────────────────────
val RadioUnselected = Color(0xFF8F8F8F)   // ring colour when unselected
val RadioSelected   = Color(0xFF4269FF)   // fill when selected
val RadioCheck      = Color(0xFFFFFFFF)   // checkmark inside selected

// ── Divider ───────────────────────────────────────────────────────────────
val DividerColor    = Color(0xFF4D4D52)

// ── Switch ────────────────────────────────────────────────────────────────
val SwitchThumb     = Color(0xFFFCFCFF)
val SwitchTrackOn   = Color(0xFF4269FF)
val SwitchTrackOff  = Color(0xFF636368)

// ── App status ────────────────────────────────────────────────────────────
val DisabledRed     = Color(0xFFD93E36)

// ── Search icon background ────────────────────────────────────────────────
val SearchIconBg    = Color(0xFF4269FF)

// ── Input boxes ───────────────────────────────────────────────────────────
val InputBoxBg      = Color(0xFF262626)
val InputTextActive = Color(0xFFFFFFFF)   // while editing / before confirm
val InputTextSaved  = Color(0xFFAEB8C1)   // after confirmed save

// ── Samsung blue (sub-label on filter chip) ───────────────────────────────
val SamsungBlue     = Color(0xFF4A9CE5)
